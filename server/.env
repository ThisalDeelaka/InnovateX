# TCP stream from Python simulator
TCP_HOST=127.0.0.1
TCP_PORT=8765

# Our Node server (Socket.IO + REST)
HTTP_PORT=4000
CORS_ORIGIN=*

# scoring thresholds (tweak live if needed)
VISION_CONFIDENCE=0.85
WEIGHT_TOLERANCE_PCT=0.07
NUDGE_THRESHOLD=0.60
HOLD_THRESHOLD=0.85

# file output
EVENTS_FILE=./data/output/events.jsonl
PRODUCTS_CSV=./data/input/products_list.csv
